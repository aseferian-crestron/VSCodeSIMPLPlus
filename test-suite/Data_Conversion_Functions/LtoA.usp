#SYMBOL_NAME "Test_LtoA"
#DEFAULT_VOLATILE

STRING_OUTPUT Code$;

FUNCTION Main()
{
    LONG_INTEGER VALUE;
    STRING temp[50];
    VALUE = 1234567890;
    Code$ = LTOA(VALUE);
    temp = LTOA(VALUE);
    PRINT("Code = %s\n", temp);
}
