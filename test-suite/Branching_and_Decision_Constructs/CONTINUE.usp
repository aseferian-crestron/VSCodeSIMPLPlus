#SYMBOL_NAME "Test_CONTINUE"
#DEFAULT_VOLATILE

ANALOG_INPUT Y;

FUNCTION Main()
{
    INTEGER X;
    
    X=0;
    WHILE(X<25)
    {
    IF(Y = 69)
    {
    CONTINUE;
    }
    PRINT( "X=%d", X );
    X = X + 1;
    }
}
