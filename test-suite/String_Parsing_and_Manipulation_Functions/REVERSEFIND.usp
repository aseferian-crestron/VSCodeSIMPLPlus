#SYMBOL_NAME "Test_REVERSEFIND"
#DEFAULT_VOLATILE

STRING_INPUT IN$[100];
INTEGER START_LOC;

CHANGE IN$
{
START_LOC = REVERSEFIND("XYZ", IN$);
PRINT("last XYZ occurrence was found at position %d in %s\n", START_LOC, IN$);
}
