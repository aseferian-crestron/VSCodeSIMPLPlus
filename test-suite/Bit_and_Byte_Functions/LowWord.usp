#SYMBOL_NAME "Test_LowWord"
#DEFAULT_VOLATILE

FUNCTION Main()
{
    LONG_INTEGER x;
    INTEGER y;
    x = 0xAABBCCDD;
    y = LowWord(x);
    Print("The LowerMost 16 Bits of %08X are %04X\n", x, y);
}
