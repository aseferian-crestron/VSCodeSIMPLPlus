#SYMBOL_NAME "Test_BREAK"
#DEFAULT_VOLATILE

ANALOG_INPUT Y;

FUNCTION Main()
{
    INTEGER X;
    
    X=0;
    WHILE(X<25)
    {
    IF(Y = 69)
    BREAK;
    X = X + 1;
    PRINT("X=%d\n", X);
    }
}
