#SYMBOL_NAME "Test_Chr"
#DEFAULT_VOLATILE

STRING_OUTPUT Code$;
ANALOG_INPUT VALUE;

CHANGE VALUE
{
    STRING temp[10];
    Code$ = CHR(VALUE);
    temp = CHR(VALUE);
    PRINT("Code = %s\n", temp);
}
