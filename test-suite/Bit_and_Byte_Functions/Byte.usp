#SYMBOL_NAME "Test_Byte"
#DEFAULT_VOLATILE

STRING_INPUT IN$[100];
CHANGE IN$
{
IF(BYTE(IN$,1) = 02)
{
SWITCH(BYTE(IN$,2))
{
CASE (65):
{
// Process Command type 65 (A) here.
}
CASE (66):
{
// Process Command type 66 (B) here.
}
}
}
}
