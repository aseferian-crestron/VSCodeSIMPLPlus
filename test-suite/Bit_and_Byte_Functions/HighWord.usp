#SYMBOL_NAME "Test_HighWord"
#DEFAULT_VOLATILE

FUNCTION Main()
{
    LONG_INTEGER x;
    INTEGER y;
    x = 0xAABBCCDD;
    y = HighWord(x);
    Print("The UpperMost 16 Bits of %08X are %04X\n", x, y);
}
